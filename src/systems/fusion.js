// REMOVED. Gu Fusion (merging two same-path/same-tier commons into a higher tier for gold +
// essence) was retired: higher-tier Gu are now obtained only by CRAFTING them, whose recipes
// already consume lower-tier Gu of the same path (see systems/crafting.js — refineFodder /
// refineCount). This file is intentionally left empty so nothing re-imports the old mechanic.
export {};
